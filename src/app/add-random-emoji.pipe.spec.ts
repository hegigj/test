import { AddRandomEmojiPipe } from './add-random-emoji.pipe';

describe('AddRandomEmojiPipe', () => {
  it('create an instance', () => {
    const pipe = new AddRandomEmojiPipe();
    expect(pipe).toBeTruthy();
  });
});
